---
import { CONFIG } from '../config.js';
---
<footer class="footer">
  <div class="wrap footer-in">
    <span>{CONFIG.marca.nombre} · {CONFIG.marca.tagline}</span>
    <span><a href={CONFIG.marca.waLink} target="_blank" rel="noopener" style="color:inherit;text-decoration:underline">WhatsApp {CONFIG.marca.whatsappLegible}</a> · {CONFIG.marca.web}</span>
    <span>{CONFIG.marca.ubicacion}</span>
  </div>
</footer>
